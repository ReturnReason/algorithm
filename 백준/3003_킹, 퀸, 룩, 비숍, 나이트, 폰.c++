#include <bits/stdc++.h>
using namespace std;

int main (){
	
	int k, q, l, b, n, p; // 1, 1, 2, 2, 2, 8
	cin >> k >> q >> l >> b >> n >> p;
	
	cout << 1 - k << " " << 1 - q << " "<< 2 - l << " "<< 2 - b << " "<< 2 - n << " " << 8 - p << "\n";
	
	return 0;
}