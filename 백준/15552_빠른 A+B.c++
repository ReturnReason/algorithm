#include <bits/stdc++.h>
using namespace std;

int main(){
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);

	int N;
	cin >> N;


	for(int i = 0; i < N; i++){
		int a, b;
		cin >> a >> b;
		cout << a + b << "\n";
	}

	return 0;
}